# Copy to .env (gitignored) and fill in after creating the Sanity project.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Robot token (Editor role) from sanity.io/manage -> project -> API -> Tokens.
# Used only by scripts/seed.ts, never shipped to the browser.
SANITY_TOKEN=
